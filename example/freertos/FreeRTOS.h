/* Simulation stub — redirects to the POSIX simulation layer.
 * On a real target this file is provided by the FreeRTOS port. */
#ifndef FREERTOS_H
#define FREERTOS_H
#include "freertos_sim.h"
#endif
